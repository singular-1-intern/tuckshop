{
  // NOTE: This configuration is layered over both appsettings.json and appsettings.Development.json
  // Only add config here to override the normal Development settings.

  // To run the app in a Docker container, use the dropdown on the run button and select 'Docker'.
  //
  // Note: Once you start the application with Docker, even if you stop it in Visual Studio, the
  // container continues to run, and will have bound to the external port (As defined in
  // launchSettings.json). If you switch back to the normal app profile, it will then fail to start.
  // To stop the container, you can exit Visual Studio, or manually terminate the running container.

  // --------------------------------------------------------------------------
  // Application Configuration
  // --------------------------------------------------------------------------
  "ConnectionStrings": {
    // Requires a local SQL user called 'docker_user' with sysadmin rights
    // Alternatively, you can give the user 'db_creator', and if you have an existing database, manually add the user to it with db_owner rights.
    "Main": "Server=host.docker.internal,1433; Database=Tuckshop.IdentityServer; User ID=docker_user; Password=S1ngul@r!500; Encrypt=True; TrustServerCertificate=True; Column Encryption Setting=Enabled; MultipleActiveResultSets=True; Persist Security Info=True;"
  },
  "Kestrel": {
    "Certificates": {
      "Default": {
        "Path": "Certificates/neo-docker.pfx",
        "Password": "P@ssw0rd"
      }
    },
    "EndPoints": {
      "Https": {
        // This is the port inside the container. The port on your local system is defined in launchSettings.json
        "Url": "https://*:443"
      }
    }
  },

  // --------------------------------------------------------------------------
  // Local Services
  // --------------------------------------------------------------------------

  // Use host.docker.internal to refer to your localhost / host machine
  "Authentication": {
    "AuthenticationAuthorityUrl": "https://host.docker.internal:5000",
    "AuthorizationUrl": "https://host.docker.internal:5000/connect/authorize",
    "TokenUrl": "https://host.docker.internal:5000/connect/token"
  },
  "AuthenticationServer": {
    "IssuerUri": "https://host.docker.internal:5000"
  },
  "NotificationServer": {
    "EndPoint": "https://host.docker.internal:5001/api/"
  }
}